export function createFollowUpDraftDto(input) {
    return {
        followUpId: input.followUpId,
        diagnosticResultId: input.diagnosticResultId,
        scheduledFor: input.scheduledFor,
        status: input.status
    };
}
//# sourceMappingURL=follow-ups.js.map