export function createDiagnosticResultDraftDto(input) {
    return {
        diagnosticResultId: input.diagnosticResultId,
        orderId: input.orderId,
        status: input.status,
        summary: input.summary
    };
}
//# sourceMappingURL=diagnostic-results.js.map