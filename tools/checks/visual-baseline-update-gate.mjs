#!/usr/bin/env node

console.error("Screenshot baseline updates are not enabled for Phase 1.");
console.error(
  "P01-FND-004 uses a deterministic visual-contract suite because no controlled cross-platform screenshot baseline environment is approved."
);
console.error("Manual design review is required before any future baseline update policy.");
process.exit(1);
