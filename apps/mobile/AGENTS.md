# Mobile Application Instructions

These instructions apply under `apps/mobile/`.

- This directory is boundary-only until P02-ISS-013 implements the Expo mobile shell.
- Do not create native feature parity, push integration, auth, clinical, payment, provider matching, production-origin behavior, or mobile runtime configuration in P02-ISS-002.
- Future mobile work must stay an empty shell unless explicitly expanded by an approved issue.
- Do not add native modules, device permissions, or push/provider integrations without explicit approval.
